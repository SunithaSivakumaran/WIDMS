USE widms;

-- Southern Province geography is maintained by
-- database/import_southern_geography_fixed.php. Do not seed sample or
-- manually spelled divisions here, because doing so makes the migration
-- conflict with the authoritative imported dataset.
